<div class="navi2">
    <% if signed_in? %>
      <a href="<%= dashboard_path %>">Dashboard</a> | 
      <%= link_to 'Logout', destroy_user_session_path, :method => :delete %>
    <% else %>
      <a href="<%= new_user_session_path %>" rel="nofollow">LogIn</a> 
    <% end %>
</div>
